# npm-demo

A minimal npm project: `package.json` with scripts, plus a local module (`greet.js`).

## Run

```bash
npm start       # runs `node server.js`
# or
node server.js
```

Then open http://localhost:8080/. Stop it with Ctrl-C.
